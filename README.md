# Rice-Leaf-disease-detection
Rice Leaf Disease Detection using CNN and Transfer Learning (VGG16) to classify rice leaf images into three disease categories. Built with TensorFlow/Keras, applying data augmentation to reduce overfitting on a small dataset. Compared custom CNN and pretrained models, achieving improved validation accuracy for precision agriculture.
